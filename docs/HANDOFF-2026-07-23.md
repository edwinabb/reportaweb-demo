# Handoff — 2026-07-23 (Cutover CISE/GRUAS)

Punto de continuación tras `/clear`. El foco cambió de Growth Engine a **cutover de los 2 clientes
reales (CISE + GRUAS) de Bubble a la v3**. Plan maestro: `docs/superpowers/plans/2026-07-22-cutover-cise-gruas.md`.

## Contexto / decisiones del día

- **Prioridad = migración/cutover, no Growth Engine.** Nadie usa la v3 aún; CISE y GRUAS operan en
  Bubble. Sin cutover completo no hay usuarios reales. Growth Engine queda pausado (specs A–E ✅).
- **Scope confirmado por el usuario:** históricamente hubo más clientes en Bubble, pero **hoy solo
  quedan activos CISE + GRUAS** → solo se migra esa info. No hace falta inventariar más tenants.
- **La migración ya está ~95% a nivel tabla** (`docs/guia_migracion_bubble_supabase.md`). El bloqueante
  real son **gaps de migración a nivel campo** que solo revela la auditoría UI ↔ datos módulo por módulo.
- **Acceso a Bubble LIVE confirmado:** `.env.local` → `BUBBLE_API_URL=https://reporta.la/api/1.1/obj`
  (live) y el token `5532c3bb…` funciona contra producción. La migración ya hecha es desde live.
  ⚠️ **Delta-sync pendiente:** live tiene ~25.678 reportes de maquinaria hoy vs ~20.678 migrados
  (Bubble sigue en uso) → re-correr migraciones idempotentes antes del cutover.
- **IDs de tenant en Bubble:** CISE `1596035803087x371442079041323000` · GRUAS `1691779382086x534175713862630160`.
- **⚠️ Bubble omite campos vacíos en la API** — todo conteo se hace sobre la unión de claves de la tabla.

## Qué se hizo hoy

1. **Plan de cutover** creado (`docs/superpowers/plans/2026-07-22-cutover-cise-gruas.md`): proceso por
   módulo de 10 pasos (screenshot → confirmar tablas → confirmar procesos → medir cobertura → matriz →
   resolver DUDAs → ejecutar críticos → template v1.2 → validar → cierre firmado) + tareas transversales
   X1–X5 + gate de go-live.
2. **Módulo 4 (Planificación) auditado** → `docs/auditoria-ui/04-planificacion.md`. Crítico TK-P1:
   8.694 recursos invisibles (`tarea_fecha_id` NULL) + 2.567 tareas sin `tareas_fechas` (incl. 123 de
   2026). DUDA-PLAN-003 (editar destruía datos) ya estaba corregida. **DUDA-PLAN-001 espera aprobación.**
3. **Terceros — 5 DUDAs resueltas con datos de Bubble LIVE** (`03-terceros.md §Resolución con Bubble LIVE`):
   los "gaps críticos" NO son pérdida de datos, existen en Bubble y son re-migrables.
   - rubro 81%, ubicacion_departamento 86%, ubicacion_ciudad 41%, condicion_seniat 69% (HABIDO),
     estado_seniat 69% (ACTIVO — el 100% en Supabase era default), tipo array multi-valor.
   - contactos cargo 95% / area 86%; sitios codigo 100% / tipo_id 100% / geodata parcial.
   - **⚠️ Corrección:** cargo/area/rubro/tipo son **IDs de opción de lista** en Bubble, no texto.
4. **Cambio de esquema Terceros** (decisión usuario): RENAME `estado`→`estadosunat`, `condicion`→
   `condicionsunat`; DROP duplicados vacíos `estado_sunat`/`condicion_sunat` + redundante `activo`
   (activo/inactivo queda en `is_active`). Migración `supabase/migrations/20260723120000_terceros_rename_estado_condicion_sunat.sql`
   (con guarda de datos) + código (`columns.tsx`, `terceros-table.tsx`) + `types/supabase.ts` actualizados.
   **⏳ PENDIENTE aplicar** (no tengo acceso de escritura a la BD).
5. **Script de re-migración** `scripts/migrate-terceros-fields.ts` (steps `rubro|ubicacion|sunat|contactos|
   sitios|all`, **dry-run por defecto**, `--apply` para escribir, upsert por bubble_id, scope CISE+GRUAS).
   Dry-run contra TEST validó: ✅ `ubicacion` (595/639) y `sunat` (441/639) = copia directa correcta.
   ⚠️ `rubro`/`contactos`/`sitios tipo` necesitan resolver el catálogo de opciones de Bubble (ver pendientes).
6. **X2 (FK valorizaciones) diagnosticado** (`guia_migracion §Diagnóstico X2`): la migración mapeó la
   dirección equivocada; el link real es `maquinaria_horas-valorizaciones.id_maquinaria_horas → reporte`
   (758 valorizaciones ~2021, huérfanas — apuntan a reportes que ya no existen en live). **Impacto bajo,
   no bloquea.** Fix opcional del lado Supabase.
7. **X5 storage (parcial):** Terceros.logo_url en Bubble 253/639 (Supabase ~248, casi todo CISE).
8. **Módulo 5 (Configuración) survey técnico:** rutas `/settings/*` + `/sistema/*`; editores de catálogo
   que reusan componentes; los catálogos son las fuentes FK (rubros, contactos_cargo/area, sitios_tipo,
   personal_cargos, terceros_tipos).

## ⭐ Actualización fin del día (2026-07-23 noche) — Terceros CERRADO en PROD

Se resolvió el acceso a la BD (el `SUPABASE_ACCESS_TOKEN` de `.env.local` es un PAT de la
**Management API** que ve test+prod; el MCP estaba con otra cuenta). Con eso se **aplicó todo**:
- **Esquema Terceros** aplicado a TEST + PROD (`estadosunat`/`condicionsunat`, drop de duplicados + `activo`).
- **Re-migración de datos a PROD (0 errores):** ubicacion 595 · sunat 441 · rubro 516 · contactos 807 ·
  sitios 1733. Catálogo de opciones resuelto vía `configuraciones_opciones_listas`.
- **Fix de build:** faltaba renombrar en `types/terceros.ts` (tipo de dominio) → corregido (commit `5a8bde4`).
- **Release v3.12.0** commiteado + push a `origin`/`demo` (deploy Cloudflare Workers Builds).

## Plan de mañana (orden sugerido)

1. **[Usuario]** Verificar que el build v3.12.0 en demo (commit `5a8bde4`) quedó verde (Cloudflare dashboard).
2. **[Usuario]** `npm run types:supabase` para regenerar `types/supabase.ts` desde la BD real
   (hoy fue hand-edit) — y correr `npm run build` local antes del próximo deploy.
3. **[Claude/Usuario]** Aprobar **DUDA-PLAN-001** (propuesta mixta A/B/C) → ejecutar **TK-P1** de
   Planificación (8.694 recursos invisibles + 2.567 tareas sin fechas).
4. **[Claude]** Continuar auditoría: **módulo 5 (Configuración/Catálogos)** — matriz formal (falta screenshots)
   o seguir con Cotizaciones/Informes. Con acceso a PROD ya se puede medir cobertura real.
5. **[Usuario/Claude]** **Delta-sync** de tablas grandes desde Bubble LIVE (Bubble sigue en uso) antes del cutover.
6. **[Opcional]** X2 valorizaciones: backfill Supabase-side (bajo impacto). Storage: PDFs/firmas faltantes.

## Pendientes (checklist de cutover)

- [x] ✅ Aplicar migración esquema Terceros (test→prod) — hecho vía Management API (`SUPABASE_ACCESS_TOKEN`).
- [x] ✅ Resolver catálogo de opciones Bubble (`configuraciones_opciones_listas`) → steps de catálogo cerrados.
- [x] ✅ Ejecutar re-migración Terceros (TK-T1..T5) `--apply` en PROD: ubicacion 595 · sunat 441 · rubro 516 · contactos 807 · sitios 1733 (0 errores). Gaps críticos de Terceros CERRADOS.
- [ ] Regenerar `types/supabase.ts` desde la BD real (`npm run types:supabase`) — hoy hand-edit.
- [ ] Aprobar DUDA-PLAN-001 → ejecutar TK-P1 (recursos invisibles Planificación).
- [ ] Ticket: `personal_cargos` sin editor central en `/settings` (solo alta inline); `terceros_tipos` catálogo muerto (enum fijo en UI).
- [ ] **Delta-sync final** de todas las tablas desde Bubble LIVE (idempotente) antes del cutover.
- [ ] X2 valorizaciones: decidir si se hace el backfill Supabase-side (opcional, bajo impacto).
- [ ] X5 storage: completar inventario (PDFs cotizaciones/facturas, firmas) + migrar faltantes.
- [ ] Screenshots de módulos 5–15 → auditorías UI formales.
- [ ] Suite E2E actualizada (DUDA-E2E-001) + verde.
- [ ] Deploy prod (worker `reportaweb-live` + apex bindeado).
- [ ] Comunicar corte a CISE/GRUAS (fecha + congelamiento Bubble).

## Acceso a la BD (resuelto durante el día)

- El **MCP Supabase** daba "permission denied" porque estaba autenticado con OTRA cuenta (solo veía
  el proyecto "alo"). El **`SUPABASE_ACCESS_TOKEN`** de `.env.local` es un PAT de la **Management API**
  que SÍ ve `reportar-test` y `reportaweb-prod`. Se usó vía
  `POST https://api.supabase.com/v1/projects/{ref}/database/query` (SQL/DDL) y `/api-keys?reveal=true`
  (service_role para el script). **Token secreto — nunca imprimir; el usuario lo usa todo el mes.**
- `PROD_SUPABASE_API_URL` sigue **comentada** en `.env.local` (sin service-role de prod fijo); para
  correr scripts contra prod se pasa la key inline obtenida por la Management API.

## Archivos clave tocados hoy

- `docs/superpowers/plans/2026-07-22-cutover-cise-gruas.md` (plan maestro)
- `docs/auditoria-ui/04-planificacion.md` (nuevo) · `03-terceros.md` (§Resolución + cambio esquema)
- `supabase/migrations/20260723120000_terceros_rename_estado_condicion_sunat.sql` (nuevo)
- `scripts/migrate-terceros-fields.ts` (nuevo)
- `types/supabase.ts`, `app/(dashboard)/terceros/columns.tsx`, `terceros-table.tsx`
- `docs/guia_migracion_bubble_supabase.md` (§Diagnóstico X2)
