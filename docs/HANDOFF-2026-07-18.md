# Handoff — 2026-07-18 (para retomar el Growth Engine)

Punto de continuación tras `/clear`. Léelo primero; luego el playbook y los specs.

## Dónde estamos

Diseñando el **Growth Engine** (leads→trial→cliente), sistema portable pensado para
reusarse en **IMPULSAR** en agosto 2026. Contexto clave: **no hay clientes en producción
en la versión nueva** (todos en Bubble) → bajo riesgo para publicar live / rotar keys.

Sub-proyectos: **A** ciclo de vida trial · **B** alta sales-led + Telegram · **C** Stripe ·
**D** onboarding post-pago · **E** asistente virtual. Orden: A→B→C→D→E.

| Doc | Estado |
|-----|--------|
| `docs/PLAYBOOK-GROWTH-ENGINE.md` | Playbook portable (arquitectura + decisiones + lecciones) |
| `docs/superpowers/specs/2026-07-18-trial-lifecycle-v2-design.md` | Spec A ✅ |
| `docs/superpowers/plans/2026-07-18-trial-lifecycle-v2.md` | Plan de implementación A ✅ (13 tasks) |
| `docs/superpowers/specs/2026-07-18-lead-provisioning-telegram-design.md` | Spec B ✅ (borrador) |
| `docs/superpowers/specs/2026-07-19-stripe-self-service-design.md` | Spec C ✅ (borrador) |
| `docs/superpowers/specs/2026-07-19-onboarding-post-pago-design.md` | Spec D ✅ (borrador) |
| `docs/superpowers/specs/2026-07-19-asistente-virtual-design.md` | Spec E ✅ (borrador) |
| `docs/RUNBOOK-ROTAR-SERVICE-ROLE-PROD.md` | Runbook rotación key (DUDA-SEC-001) |
| `C:\tmp\PROMPT-rotacion-key-publicar-live.md` | Prompt para instancia paralela (rotación + publicar live) |

## Decisiones cerradas (no reabrir sin motivo)

1. **Single-schema multi-tenant** (lead = tenant con `trial_status`), NO esquema aparte.
2. **Ciclo 15+7+3** (ACTIVE 15d → DECISION 7d → WARNING 3d → auto-borrado). Todas las
   fases con **acceso total**; el estado controla comunicación, no permisos. **SIN modo
   solo-lectura.**
3. **Onboarding adaptativo al avance real** (felicita al adelantado, recuerda al atrasado,
   tramo avanzado para el que termina antes).
4. **Telegram para el admin** (alertas + aprobación 1-clic), **web+correo para el cliente**;
   WhatsApp futuro.
5. **Editor de plan para SuperAdmin** en `/sistema/plan-onboarding` (§9 del spec A).

## Próximos pasos (elegir al retomar)

1. **Revisar** el spec A (§3 motor adaptativo, §9 editor) y el plan A — ajustar si hace falta.
2. **Implementar A** con el plan: `superpowers:subagent-driven-development` (recomendado) o
   `executing-plans`. Empezar por Task 1 (migración) en Supabase TEST.
   - ⚠️ Task 1 Step 2: verificar FK cascada de `companies` antes de confiar en el DELETE.
   - ⚠️ Task 4/5: confirmar nombres reales de tablas (`valorizaciones`, `terceros`) en `types/supabase.ts`.
3. **Specs C/D/E ✅ + planes de implementación ✅** (2026-07-19):
   `docs/superpowers/plans/2026-07-19-stripe-self-service.md`,
   `...-onboarding-post-pago.md`, `...-asistente-virtual.md`. Orden de construcción:
   A → C → D → (B en cualquier momento tras A) → E. Decisiones cerradas para C: planes
   STANDARD $29.99 / OWN BRAND $49.99 por asiento, mín. 10, USD, semestral −10% / anual
   −20%, cuotas por plan = asientos pagados, badge de ahorro visible.
4. **Contenido del plan** (copy de los 15+7 correos de A y del tramo `paid` de D) — definir
   con negocio.

## Pendientes operativos (fuera del diseño)

- **Rotación key PROD (DUDA-SEC-001):** el usuario ya creó `sb_secret_`. Falta: pasar el
  `sb_publishable_` para actualizar `NEXT_PUBLIC_SUPABASE_ANON_KEY` en `.env.production`, y
  **deshabilitar las legacy** (seguro: nadie las usa en prod). Runbook actualizado con la
  Ruta B simplificada pendiente de aplicar. Al cerrar → marcar DUDA-SEC-001 ✅.
- **Publicar live:** bajo riesgo (sin clientes). Requiere secret `SUPABASE_SERVICE_ROLE_KEY`
  (=`sb_secret_`) en worker `reportaweb-live` + enrutar dominio. Decisión: publicar para
  pruebas internas, no para leads hasta tener A+B implementados.
- **Footer de versión:** ya arreglado (deriva de `package.json`); se verá al próximo deploy.
- **Crons en Cloudflare:** el cron `trial-lifecycle` (Task 8) es el primero a montar; la
  migración de crons Vercel→Cloudflare sigue pendiente (TECHNICAL_DEBTS).

## Estado git

Rama `master`. Todo committeado. Sin cambios de versión (v3.11.4 sigue vigente; el
Growth Engine no se ha desplegado — es diseño + plan).
