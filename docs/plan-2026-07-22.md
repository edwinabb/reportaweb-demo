# Plan 2026-07-22 — Remediación de secretos expuestos (tokens)

**Fecha:** 2026-07-22
**Foco:** cerrar la limpieza de secretos en texto plano detectada en los settings de
Claude Code y ampliada hoy (tokens de Bubble filtrados a git + docs).
**Runbook base:** [RUNBOOK-ROTAR-TOKENS-EXPUESTOS.md](./RUNBOOK-ROTAR-TOKENS-EXPUESTOS.md)
**Registro persistente:** [TECHNICAL_DEBTS.md](./TECHNICAL_DEBTS.md) (DUDA-SEC-001/002/003)

> ⚠️ **Regla:** las revocaciones/rotaciones en dashboards las ejecuta el usuario.
> Claude no deshabilita ni rota keys sin confirmación explícita. Sacar un token de
> un archivo **no lo invalida** — sigue vivo hasta el *Revoke* en el dashboard.

---

## 1. Hecho hoy (✅ ejecutado en sesión)

| # | Acción | Resultado |
|---|--------|-----------|
| A | Borrar bloque `mcpServers.supabase` de `~/.claude/settings.json` | ✅ quitó PAT `sbp_2135…` (MCP estaba sin uso: 0 llamadas; el acceso real es el conector Supabase de claude.ai vía OAuth) |
| B | Borrar regla de permiso con `sbp_bd7c…` de `~/.claude/settings.json` | ✅ |
| C | Borrar 2 reglas curl con Bubble `2539…` de `reportaweb3/.claude/settings.json` | ✅ |
| — | Validar JSON + `grep` de verificación | ✅ ambos JSON válidos, sin secretos `sbp_`/Bubble/`BcJ` |
| D | Revocar `sbp_6fb2…` (CLI) + reemplazar por token nuevo en `.env.local` | ✅ (usuario) — verificado con `npx dotenvx run -f .env.local -- npx supabase gen types…` |
| E | Revocar `sbp_4832…` | ✅ (usuario) — confirmado huérfano (sin referencias locales, sin CI, deploys usan service_role no PAT) |
| F | Borrar `.env - copia.local` (copia con secreto) | ✅ (usuario) |

---

## 2. Por ejecutar hoy (pendiente)

### 2.1 Revocar en Supabase Dashboard (usuario)
- [ ] Revocar PAT `sbp_2135…c4fa0` (era del MCP)
- [ ] Revocar PAT `sbp_bd7c…b370d2` (era de la regla de tipos)
- Ruta: https://supabase.com/dashboard/account/tokens

### 2.2 Rotar tokens de Bubble — **ampliación de hoy**
Hallazgo nuevo: **hay dos tokens de Bubble y ambos están filtrados a git**.
- `2539…dbd9` → estaba en `settings.json` (commits `eae2fbb`, `5463816`).
- `5532c3bb…` → el que usa la migración (`.env.local` → `BUBBLE_API_TOKEN`), **hardcodeado
  en 4 docs trackeados** y en git (commits `162a503`, `5463816`).

Ningún código lee `BUBBLE_API_TOKEN` desde `.env.local`: el acceso a Bubble se hacía con
el token **inline en curls**. De aquí en más el token vive **solo** en `.env.local` y se
pasa por env (`curl -H "Authorization: Bearer $BUBBLE_API_TOKEN" "$BUBBLE_API_URL/..."`,
cargando `.env.local` con `npx dotenvx run -f .env.local -- …`).

- [ ] **Bubble** (`reporta.la` → Settings → API → API Tokens): generar **uno nuevo**,
      revocar los dos viejos (`2539…` y `5532c3bb…`). *(usuario)*
- [ ] Poner el nuevo en `.env.local` → `BUBBLE_API_TOKEN` (sin pasarlo por chat). *(usuario)*
- [x] **Limpiar `5532c3bb…` de los 4 docs trackeados** ✅ (2026-07-22, Claude) — reemplazado
      por placeholder `<BUBBLE_API_TOKEN — ver .env.local>`. `git grep 5532c3bb` vacío.
  - `docs/migracion-mapeo-bubble-supabase.md`
  - `docs/julio12/migracion-mapeo-bubble-supabase.md`
  - `docs/julio12/README.md`
  - `docs/julio12/KICKOFF_v3.11_2026-07-15.md`

### 2.3 Higiene opcional
- [x] Desinstalar el `claude` npm-global duplicado ✅ (2026-07-22) — `removed 2 packages`;
      binario de `AppData/Roaming/npm` eliminado; activo sigue siendo el native `~/.local/bin/claude`

---

## 3. Arrastre — NO perder en planes subsiguientes

Registrado en [TECHNICAL_DEBTS.md](./TECHNICAL_DEBTS.md) para que sobreviva a este plan:

| ID | Tema | Prioridad | Dónde se cierra |
|----|------|-----------|-----------------|
| **DUDA-SEC-001** | Rotar SERVICE_ROLE_KEY de PROD; migrar a `sb_secret_` + deshabilitar legacy | ALTA | 🟢 anon→`sb_publishable` ✅ (v3.11.5); falta `sb_secret_` en worker + deshabilitar legacy → §5 |
| **DUDA-SEC-002** | Tokens Bubble (`2539…`, `5532c3bb…`) filtrados a git + 4 docs → rotar + limpiar docs | ALTA | §2.2 de este plan |
| **DUDA-SEC-003** | Revocar en dashboard los PAT Supabase ya removidos de settings (`sbp_2135`, `sbp_bd7c`) | ALTA | §2.1 de este plan |
| **Runbook #3** | Password BD Postgres de **SerIA** (`BcJ*…`) expuesto en settings — otro proyecto | ALTA | Runbook § 3 (usuario, en SerIA) |
| **Runbook #5** | JWT `anon` en reglas curl (públicas por diseño) | BAJA | Opcional |

**Historia de git:** rotados los tokens, las copias en commits viejos quedan **inertes**.
Con repos privados alcanza; reescribir historia es opcional y más invasivo (evaluar solo si
algún repo pasa a público).

---

## 4. Verificación final (tras rotar y editar)

```bash
# 1) sin secretos en settings
grep -REn "sbp_|sb_secret_|BcJ\*19n104|2539208d3baeaade|5532c3bb4891ccf5" \
  "$HOME/.claude/settings.json" "C:/Proyectos/reportaweb3/.claude/settings.json" \
  "C:/Proyectos/reportaweb3/.claude/settings.local.json"
# 2) sin token Bubble viejo en docs trackeados
git grep -n "5532c3bb4891ccf5" || echo "OK docs limpios"
# 3) CLI Supabase sigue funcionando con el token nuevo
npx dotenvx run -f .env.local -- npx supabase gen types typescript \
  --project-id oyrokyyaeaeqzlsgxtto --schema public > types/supabase.ts && echo "OK tipos"
```
Esperado: (1) y (2) sin resultados; (3) genera tipos sin error de auth.

---

## 5. Track paralelo — go-live + estándar de dominios (2026-07-22)

> Trabajado en instancia separada (operativo/deploy), en paralelo a la remediación de
> secretos de arriba. Aquí queda el registro para no perderlo.

### Hecho ✅
- **Limpieza de código muerto** (auditoría knip + verificación cruzada): 18 archivos + 27
  exports sin uso + 1 archivo basura U+F03A. Gate `tsc` verde. Commit `b766b8a` (en `master`).
  Detalle: [DEAD-CODE-AUDIT-2026-07-21.md](./DEAD-CODE-AUDIT-2026-07-21.md).
- **Rotación anon PROD → `sb_publishable_`** en `.env.production` + `wrangler.live.toml`
  (release **v3.11.5**, commit `f7a9417`). Pusheado a `origin`/`demo`/`live`. Demo desplegó.
- **Decisión de costos:** worker remoto **4.86 MB gzip** > Free (1 MB) → **Workers Paid**
  ($5/mes, por cuenta, cubre dev+prod). Prod necesita Paid igual por CPU 10ms/req del Free.
- **Estándar de dominios unificado** (commit `6e5085e`): `reportar.app` apex = home con login
  embebido + `/novedades` + landings de campaña + `/registro` + app · `dev.reportar.app` interno
  · `www`→301 · env-driven site URL (fix del fallback muerto `web.reportar.app`). Doc en
  [ARCHITECTURE.md § Domain & Routing Standard] + playbook portable
  [PLAYBOOK-DOMAIN-STRATEGY.md](./PLAYBOOK-DOMAIN-STRATEGY.md).

### Pendiente ⏳
- **Cloudflare (usuario):** crear entorno de prod (`reportar.app` apex) importando repo
  `reportaweb-live` a Workers Builds + cargar secret `sb_secret_` + bindear apex + rebind
  `demo`→`dev.reportar.app` + `www`→301. (Cierra DUDA-SEC-001 al verificar y deshabilitar legacy.)
- **Marca/marketing:** prefijo de landings de campaña (candidatos `soluciones/`, `conoce-`).
- **Growth Engine (ventas):** diseño de home/novedades/landings + UX trial secuencial.
- **Claude (cuando exista la home):** `/login` → 301 `/` + middleware redirige no-auth a `/`.
